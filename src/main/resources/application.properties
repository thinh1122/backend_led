spring.application.name=backend_led
server.port=${PORT:8080}

# PostgreSQL Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d6l441fpm1nc7394nqeg-a.singapore-postgres.render.com:5432/backend_led_5n2w}
spring.datasource.username=${DB_USERNAME:backend_user}
spring.datasource.password=${DB_PASSWORD:TDVcQlEtLKcJfKyTtW8UTAPsrs0BjNyg}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# MQTT Configuration
mqtt.broker=${MQTT_BROKER_URL:tcp://broker.hivemq.com:1883}
mqtt.client-id=java_backend_led
mqtt.username=${MQTT_USERNAME:}
mqtt.password=${MQTT_PASSWORD:}
